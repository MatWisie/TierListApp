<UserControl x:Class="TierListApp.Views.AddTierListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:TierListApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Label Content="Create new tier list" VerticalAlignment="Center" FontSize="25"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Label Content="Name your tier list" VerticalAlignment="Center" FontSize="20"/>
            <TextBox Text="{Binding TierListName, Mode=TwoWay}" Width="400" Margin="5"/>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal">
                <Label Content="Create tiers" FontSize="20" VerticalAlignment="Center"/>
                <Button Content="+" FontSize="30" Width="40" Background="Green" Command="{Binding AddNewTierCommand}"/>
            </StackPanel>
            <ListView ItemsSource="{Binding ListOfTiers}" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="5*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.Background>
                                <SolidColorBrush Color="{Binding TierColor, Mode=TwoWay}"/>
                            </Grid.Background>
                            <TextBox Text="{Binding Name, Mode=TwoWay}" Width="40" FontSize="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                            <toolkit:ColorPicker Grid.Column="1" Margin="5 0 0 0" SelectedColor="{Binding TierColor, Mode=TwoWay}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Content="Create" FontSize="25" HorizontalAlignment="Left" Background="LightGreen" Margin="10" Command="{Binding CreateTierListCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
